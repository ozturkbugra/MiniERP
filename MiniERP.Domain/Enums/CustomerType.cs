namespace MiniERP.Domain.Enums
{
    public enum CustomerType
    {
        Buyer = 1,      // Sadece Müşteri (Alıcı)
        Supplier = 2,   // Sadece Tedarikçi (Satıcı)
        Both = 3        // Hem Alıcı Hem Satıcı
    }
}
